//ARRAYS EN JAVA

/**
 * 
 * Los ARRAYS son colecciones de datos que agrupan una serie de variables del mismo tipo.
 * Cuando creamos un ARRAY se reserva automáticamente memoria, 
 * al iniciar un ARRAY debemos indicar su tipo y tamaño, 
 * este tamaño no puede variar durante la ejecución
 * 
 * 
 * @author monts
 *
 *Para crear un ARRAY usamos NEW, veamos su sintaxis para definirlo:
 *Tipo_dato nombre_array[]=new tipo_dato[tamaño];
 *
 */
public class TestApp20 {

	public static void main(String[] args) {
		// Definimos un array con 10 posiciones
		int num[]=new int [10];
		
		//Al tener un ARRAY creado, se rellenan por defecto por ejemplo si es un ARRAY de int
		//se rellenan con 0, si es array de String se rellenan con NULL, ya que Strin es una clase en reallidad
		
		

	}

}
